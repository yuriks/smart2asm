; Level Data
{% for labels, level_data in data.compressed_level_data %}

{% for l in labels %}
{{ l }}:
{% endfor %}
    {% set path = "level_data/" + labels | first + ".bin" %}
    incbin "{{ level_data | write_file(path, compress=true) }}"
{% endfor %}