; PLM Populations
{% for labels, population in data.plm_populations %}

{% for l in labels %}
{{ l }}:
{% endfor %}
{% for plm in population %}
    {% set param = plm.param.ScrollDataUpdate | default(plm.param.Value) %}
    %PLMPopEntry({{ symbols.resolve_label(0x84, plm.plm_header) }}, {{ plm.pos_x }}, {{ plm.pos_y }}, {{ param }})
{% endfor %}
    dw $0000
{% endfor %}